{
  "name": "random-word-generator-site",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.12.0" },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "astro build",
    "preview": "php -S localhost:8080 -t dist",
    "test": "node --test tests/*.test.mjs",
    "test:build": "node --test tests/site-build.mjs",
    "check:examples": "python3 database/check_examples.py"
  },
  "dependencies": {
    "astro": "^7.3.3"
  }
}
